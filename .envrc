# .envrc - Environment variables for direnv
# This file is loaded automatically when you enter the project directory
# Install direnv: https://direnv.net/docs/installation.html

# Load .env if it exists (for local development)
if [[ -f .env ]]; then
  dotenv
  
  # Compute derived variables
  export SERVICE_ACCOUNT_EMAIL="${GITHUB_SERVICE_ACCOUNT_NAME}@${GCP_PROJECT_ID}.iam.gserviceaccount.com"
  
  echo "🚀 Environment loaded for ${GCP_SERVICE_NAME} (${GCP_PROJECT_ID})"
  echo "📍 Region: ${GCP_REGION}"
else
  echo "⚠️  .env file not found. Copy .env.example to .env and configure your settings."
fi
